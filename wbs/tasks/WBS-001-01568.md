---
id: WBS-001-01568
title: 生成UGEの検証に必要な解析結果の契約を定義する
type: task
status: complete
actor: codex
parent: WBS-001-01566
depends_on:
- WBS-001-01567
specs:
- docs/json-format.md
- docs/sound-spec.md
related_files:
- tools/analyze_uge.py
- tests/test_analyze_uge.py
outputs:
- docs/generated-uge-analysis-contract.md
blocked_by: []
source_wbs: []
evidence_required: true
updated: '2026-08-01'
---

# 目的

既存`analyze_uge.py::read_file()`の解析結果を生成UGEの構造検証へ再利用するため、必要field・意味・不足範囲を契約として定義する。
# 作業内容

新しい汎用中間表現を先に作らず、既存read_fileの結果を生成UGEの構造検証へ利用する。追加が必要な場合だけ、入力・出力・未対応範囲を定義する。
# 入力

生成UGE検証用の項目契約と不足機能の追加方針。
# 成果物

生成UGE検証用の`read_file()`結果契約。
# 完了条件

既存結果の再利用可否と、追加が必要な最小項目を記録する。
# 完了にしてはいけない条件

人による確認が必要な場合、確認結果がない状態で完了にしない。
# 確認結果

2026-09-25、`read_file()`のorder/pattern/channel/loop/event/error結果を確認し、01569必須のraw cellと64-row確認には公開結果が不足していたため、既存内部patternを`pattern_cells`として最小公開した。新しい汎用IRは追加していない。JSON loop metadataとUGE B effectの意味は分離し、Instrument音色・聴感・GUI・実機確認は対象外とした。
# 証跡

`docs/generated-uge-analysis-contract.md`、`tools/analyze_uge.py`、`tests/test_analyze_uge.py`、`python3 -m unittest tests.test_analyze_uge`。
