# WBS移行棚卸し

2026-08-01にPROJECT.mdのチェックリストを出現順に機械解析した。

|項目|件数|
|---|---:|
|移行前総数|673|
|完了|506|
|未完了|167|
|最大階層|7|
|親項目|117|
|末端項目|556|
|[Codex]表記|23|
|[人]表記|4|
|担当表記なし|646|

新IDは `WBS-001-01000` から `WBS-001-01672` まで、PROJECT.mdの出現順で対応付けた。移行後はgroup 117件、task 556件、完了task 414件、未完了task 142件である。タイトル重複は同一親配下を含め検証ツールの警告対象とした。既存項目に明示的な依存関係はなく、依存先は空配列で移行した。

親が完了なのに未完了子孫を含む既存矛盾を検出した。移行では状態を変更せず、検証ツールは警告として報告する。`[Codex]` と `[人]` の明示的な混在はグループ配下に分割済みであり、移行時に完了項目を再分割していない。個別の完了条件・証跡・参照仕様を元チェックリストから確定できない項目は、各ファイルに「未登録」と記録した。

## 移行対応

全673項目を `wbs/tasks/WBS-001-01000.md`～`WBS-001-01672.md`へ1対1で移行した。詳細な対応表は `reports/wbs.xlsx` の「移行対応」に収録する。分割・統合はなし。`source_wbs` は既存項目に由来情報がないため全項目空配列とした。

## 未確定事項

元WBSに担当・依存・完了条件・証跡が明記されない項目は推測せず未登録とした。人によるGUI、実機、エミュレータ、試聴確認は自動確認で完了扱いにしない。
